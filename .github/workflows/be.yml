name: Deploy Backend

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**' # Tự động chạy khi bạn sửa bất kỳ file nào trong folder backend

jobs:
  deploy-be:
    name: 🚀 Deploy Backend to InfinityFree
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync Backend files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftpupload.net
          username: if0_40701573
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./backend/
          server-dir: /htdocs/backend/ # Dấu / ở đầu cực kỳ quan trọng để định vị đúng htdocs