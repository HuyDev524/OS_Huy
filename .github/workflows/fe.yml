name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**' # Tự động chạy khi bạn sửa file trong folder frontend

jobs:
  deploy-fe:
    name: 🎨 Deploy Frontend to InfinityFree
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync Frontend files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftpupload.net
          username: if0_40701573
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./frontend/
          server-dir: /htdocs/ # Đảm bảo file index.html nằm đúng htdocs/frontend/